{% extends 'base.html.twig' %}
{% block body %}
    <main>
        <section>
            <div>
            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
            <a href="{{path('proJourney')}}"><i class="fas fa-undo"></i></i></a>
            </div>
        </section>
    </main>
{% endblock %}